import { ReactNode } from 'react';
/**
 * Renders global children from useCanvas hook
 */
declare const GlobalChildren: ({ children }: {
    children?: ReactNode;
}) => JSX.Element;
export default GlobalChildren;
